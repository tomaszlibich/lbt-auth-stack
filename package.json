{
  "name": "lbt-auth-stack",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "authentication-service",
    "authorization-service",
    "healthcheck"
  ],
  "scripts": {
    "build": "npm run build -w authentication-service && npm run build -w authorization-service",
    "build:auth": "npm run build -w authentication-service",
    "build:authz": "npm run build -w authorization-service",
    "dev:auth": "npm run dev -w authentication-service",
    "dev:authz": "npm run dev -w authorization-service"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.614.0",
    "@aws-sdk/lib-dynamodb": "^3.614.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
